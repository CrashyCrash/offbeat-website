# Disabled Workflows

This folder preserves old workflow files outside GitHub's active workflow path.

Files here are historical only and must not be copied back into
`.github/workflows/` without explicit user approval.
